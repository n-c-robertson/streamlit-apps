#========================================
# ASSESSMENT CREATOR - REQUIREMENTS.TXT
#========================================

# Core Web Framework
streamlit>=1.28.0

# Data Science & Analysis
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Data Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.15.0

# Machine Learning & AI
openai>=1.0.0

# HTTP Requests
requests>=2.31.0

# Standard Library (included with Python)
# ast, concurrent.futures, hashlib, json, os, pickle, random, re, sys, time, traceback, collections
# These are part of Python's standard library and don't need to be installed separately

#========================================
# INSTALLATION NOTES
#========================================
# 
# To install all dependencies:
# pip install -r requirements.txt
#
# For development environment:
# pip install -r requirements.txt --upgrade
#
# Note: Some packages may have system-level dependencies
# (e.g., matplotlib may require additional system libraries on Linux)
#
#======================================== 