# Template for .streamlit/secrets.toml. Copy to secrets.toml and edit.
# On Streamlit Cloud, paste these into app settings → Secrets instead.

# Which environment the radio button defaults to ("prod" or "staging").
default_environment = "prod"
